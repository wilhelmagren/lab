[project]
name = "how-query-engines-work"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wilhelmagren", email = "wilhelmagren98@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.5.5",
    "polars>=1.44.1",
    "pyarrow>=25.0.1",
]

[project.scripts]
how-query-engines-work = "how_query_engines_work:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
